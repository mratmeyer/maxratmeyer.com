<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

    {% if title == "Max Ratmeyer's Blog" %}
        <title>{{ renderData.title or title or metadata.title }}</title>
    {% else %}
        <title>{{ renderData.title or title or metadata.title }} - Max Ratmeyer's Blog</title>
    {% endif %}
    <meta name="description" content="{{ renderData.description or description or metadata.description }}"/>

    <link rel="preconnect" href="//static.maxratmeyer.com/" crossorigin>
    <link rel="dns-prefetch" href="//cdnjs.cloudflare.com">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" integrity="sha256-eZrrJcwDc/3uDhsdt61sL2oOBY362qM3lon1gyExkL0=" crossorigin="anonymous" />

    {% set css %}
    {% include "style.css" %}
    {% endset %}
    
    <style>
        {{css | cssmin | safe}}
    </style>

    {% set absoluteUrl %}{{ page.url | url }}{% endset %}
    <link rel="canonical" href="https://www.maxratmeyer.com{{ absoluteUrl }}"/>

    <link rel="icon" type="image/png" sizes="32x32" href="{{ static.prefix }}icons/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="{{ static.prefix }}icons/favicon-16x16.png">
    
</head>