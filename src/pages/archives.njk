---
title: Archives
layout: base
permalink: /blog/
description: Archives from Max Ratmeyer's Blog
---
<h1 class="page-title">{{ title }}</h1>
{%- for post in collections.posts | reverse -%}
    <div class="article-header article-preview-header article-archives-header">
        <p class="date">{{ post.data.date | dateformat }}</p>
        <div class="tag-container">
            {%- for tag in post.data.tags -%}
                {% if (tag != "posts") %}    
                    <div class="tag">
                        <a class="tag-link" href="/tags/{{ tag }}">
                            <p class="tags">{{ tag }}</p>
                        </a>
                    </div>
                {% endif %}
            {%- endfor -%}
        </div>
        <p class="title">
            <a class="title" href="{{ post.url | url }}">{{ post.data.title }}</a>
        </p>
    </div>
{%- endfor -%}
